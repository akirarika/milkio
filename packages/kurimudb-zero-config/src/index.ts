export { cookie } from './cookie';
export { db } from './db';
export { local } from './local';
export { memory } from './memory';

export {
  auto$,
  batch$,
  makeKMap,
  KMap,
  clone,
  globalConfig,
  runtime,
} from 'kurimudb';
